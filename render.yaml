services:
  - type: web
    name: facilita-ai-crm
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: MINHA_API_KEY
      - key: SERPAPI_KEY
      - key: OPENROUTER_KEY
