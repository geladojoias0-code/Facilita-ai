services:
  - type: web
    name: facilita-api
    env: python
    runtime: python312
    region: oregon
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DB_PATH
        value: ./facilita_ai.db